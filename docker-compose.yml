version: '3.8'

services:
  db:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: movie-db
    environment:
      ACCEPT_EULA: "Y"
      SA_PASSWORD: "${DB_PASSWORD}"
    ports:
      - "1433:1433"
    volumes:
      - sql_data:/var/opt/mssql
    networks:
      - movie_network
    healthcheck:
      test: ["CMD-SHELL", "/opt/mssql-tools/bin/sqlcmd -S localhost -U sa -P \"$SA_PASSWORD\" -Q \"SELECT 1\" >/dev/null 2>&1"]
      interval: 10s
      timeout: 5s
      retries: 10
    restart: unless-stopped

  api:
    build: .
    container_name: movie-api
    environment:
      ConnectionStrings__DefaultConnection: "Server=db,1433;Database=MovieDb;User Id=sa;Password=${DB_PASSWORD};TrustServerCertificate=True"
      ASPNETCORE_URLS: "http://+:8080"
    networks:
      - movie_network
    restart: unless-stopped

  dbgate:
    image: dbgate/dbgate
    container_name: movie-dbgate
    restart: unless-stopped
    ports:
      - "8081:3000"
    networks:
      - movie_network


  proxy:
    image: nginx:latest
    container_name: movie-proxy
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro,Z
    depends_on:
      - api
    networks:
      - movie_network
    restart: unless-stopped

volumes:
  sql_data: {}

networks:
  movie_network:
    driver: bridge
